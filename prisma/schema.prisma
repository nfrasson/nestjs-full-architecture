generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model User {
  userId        String @id
  userEmail     String @unique
  userFirstname String
  userLastname  String
  userPassword  String
}
